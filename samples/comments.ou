a, b: float;
c, d: int;

{
    a = 5 * 3.2;    /*  a is 5 times 3.2 */
    c = 5 * 3;
    b = static_cast<float>(c);  /* static_cast of c to float and assign to b */

    /*
        This is a static cast
        of a to int
        which is assigned to d
    */
    d = static_cast<int>(a);
}